<section class="relative bg-gray-900 overflow-hidden h-[500px] md:h-[600px] flex items-center">
    {# Background Image #}
    <div class="absolute inset-0 z-0">
        <img src="{{ 'images/hero-bg.jpg' | asset }}" alt="Hero Background" class="w-full h-full object-cover opacity-60">
        <div class="absolute inset-0 bg-gradient-to-t from-gray-900 via-transparent to-transparent"></div>
    </div>

    <div class="container relative z-10">
        <div class="max-w-2xl text-white">
            <h1 class="text-4xl md:text-6xl font-bold leading-tight mb-6 animate-slideUp">
                {{ trans('blocks.home.hero_title')|default('اكتشف تشكيلتنا الجديدة') }}
            </h1>
            <p class="text-lg md:text-xl text-gray-200 mb-8 leading-relaxed animate-slideUp" style="animation-delay: 0.1s;">
                {{ trans('blocks.home.hero_description')|default('تسوق أحدث المنتجات العصرية بجودة عالية وأسعار منافسة.') }}
            </p>
            <div class="flex flex-wrap gap-4 animate-slideUp" style="animation-delay: 0.2s;">
                <a href="{{ link('/products') }}" class="btn btn--primary btn--rounded-full px-8 py-4 text-lg min-w-[160px]">
                    {{ trans('blocks.home.shop_now')|default('تسوق الآن') }}
                </a>
                <a href="{{ link('/offers') }}" class="btn btn--outline light btn--rounded-full px-8 py-4 text-lg min-w-[160px]">
                    {{ trans('blocks.home.view_offers')|default('العروض') }}
                </a>
            </div>
        </div>
    </div>
</section>
