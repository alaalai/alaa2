<section class="py-12 bg-white border-b border-gray-100">
    <div class="container">
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
            {# Feature 1 #}
            <div class="flex items-center gap-4 p-6 rounded-2xl bg-gray-50 hover:bg-white hover:shadow-md transition-all duration-300 border border-transparent hover:border-gray-100 group">
                <div class="w-12 h-12 rounded-full bg-blue-100 text-blue-600 flex items-center justify-center text-2xl group-hover:scale-110 transition-transform duration-300">
                    <i class="sicon-shipping"></i>
                </div>
                <div>
                    <h3 class="font-bold text-gray-900">{{ trans('blocks.features.fast_shipping', 'Fast Shipping') }}</h3>
                    <p class="text-sm text-gray-500">{{ trans('blocks.features.fast_shipping_desc', 'Delivery within 24 hours') }}</p>
                </div>
            </div>

            {# Feature 2 #}
            <div class="flex items-center gap-4 p-6 rounded-2xl bg-gray-50 hover:bg-white hover:shadow-md transition-all duration-300 border border-transparent hover:border-gray-100 group">
                <div class="w-12 h-12 rounded-full bg-green-100 text-green-600 flex items-center justify-center text-2xl group-hover:scale-110 transition-transform duration-300">
                    <i class="sicon-security-check"></i>
                </div>
                <div>
                    <h3 class="font-bold text-gray-900">{{ trans('blocks.features.secure_payment', 'Secure Payment') }}</h3>
                    <p class="text-sm text-gray-500">{{ trans('blocks.features.secure_payment_desc', '100% secure payment') }}</p>
                </div>
            </div>

            {# Feature 3 #}
            <div class="flex items-center gap-4 p-6 rounded-2xl bg-gray-50 hover:bg-white hover:shadow-md transition-all duration-300 border border-transparent hover:border-gray-100 group">
                <div class="w-12 h-12 rounded-full bg-purple-100 text-purple-600 flex items-center justify-center text-2xl group-hover:scale-110 transition-transform duration-300">
                    <i class="sicon-headset"></i>
                </div>
                <div>
                    <h3 class="font-bold text-gray-900">{{ trans('blocks.features.support', '24/7 Support') }}</h3>
                    <p class="text-sm text-gray-500">{{ trans('blocks.features.support_desc', 'Dedicated support') }}</p>
                </div>
            </div>

            {# Feature 4 #}
            <div class="flex items-center gap-4 p-6 rounded-2xl bg-gray-50 hover:bg-white hover:shadow-md transition-all duration-300 border border-transparent hover:border-gray-100 group">
                <div class="w-12 h-12 rounded-full bg-orange-100 text-orange-600 flex items-center justify-center text-2xl group-hover:scale-110 transition-transform duration-300">
                    <i class="sicon-money-bill"></i>
                </div>
                <div>
                    <h3 class="font-bold text-gray-900">{{ trans('blocks.features.money_back', 'Money Back') }}</h3>
                    <p class="text-sm text-gray-500">{{ trans('blocks.features.money_back_desc', 'Guarantee within 7 days') }}</p>
                </div>
            </div>
        </div>
    </div>
</section>
